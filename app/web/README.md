Start it with:

uvicorn app.web.app:app --reload

Then open: 
http://localhost:8000. 

The layout:
* Search bar at the top — type a symbol and hit Enter or Search
* 8 cards in a responsive grid, one per table
* Numbers are auto-formatted (percentages, large numbers abbreviated to B/T, timestamps to dates)
* Dark theme with clean typography